{{ if .Values.overserr.enabled }}
---
### CONFIGMAP
## INIT-CONTAINER
---
apiVersion: v1
data:
  config.xml: |
    <Config>
      <UrlBase>{{ .Values.overserr.ingress.path }}</UrlBase>
    </Config>
  init-overserr.sh: |
    #!/bin/bash
    echo "### Initializing config ###"
    if [ ! -f /overserr-config/config.xml ]; then
      cp -n /init-overserr/config.xml /overserr-config/config.xml
      echo "### No configuration found, intialized with default settings ###"
    fi
kind: ConfigMap
metadata:
  name: init-overserr-cm
---
## APPLICATION
apiVersion: v1
kind: ConfigMap
metadata:
  name: overserr-config
data:
  PGID: "{{ .Values.general.pgid }}"
  PUID: "{{ .Values.general.puid }}"
---
### DEPLOYMENT
apiVersion: apps/v1
kind: Deployment
metadata:
  name: overserr
  labels:
    {{- include "k8s-mediaserver.labels" . | nindent 4 }}
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      {{- include "k8s-mediaserver.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "k8s-mediaserver.selectorLabels" . | nindent 8 }}
        app: overserr
    spec:
      initContainers:
        - name: config-overserr
          image: docker.io/ubuntu:groovy
          command: ["/init-overserr/init-overserr.sh"]
          volumeMounts:
            - mountPath: /init-overserr
              name: init-files-overserr
          {{- if .Values.overserr.volume }}
            - name: {{ .Values.overserr.volume.name }}
              mountPath: /overserr-config
          {{- else }}
            - name: mediaserver-volume
              mountPath: /overserr-config
              subPath: "{{ .Values.general.storage.subPaths.config }}/overserr"
          {{- end }}
          securityContext:
            runAsUser: {{ .Values.general.puid }}
            runAsGroup: {{ .Values.general.pgid }}
      containers:
        - name: {{ .Chart.Name }}
          envFrom:
            - configMapRef:
                name: overserr-config
          image: "{{ .Values.overserr.container.image }}:{{ .Values.overserr.container.tag | default .Values.general.image_tag }}"
          imagePullPolicy: Always
          readinessProbe:
            tcpSocket:
              port: {{ .Values.overserr.container.port }}
            initialDelaySeconds: 10
            periodSeconds: 20
          ports:
            - name: overserr-port
              containerPort: {{ .Values.overserr.container.port }}
              protocol: TCP
          volumeMounts:
          {{- if .Values.overserr.volume }}
            - name: {{ .Values.overserr.volume.name }}
              mountPath: /config
          {{- else }}
            - name: mediaserver-volume
              mountPath: "/config"
              subPath: "{{ .Values.general.storage.subPaths.config }}/overserr"
          {{- end }}
            - name: mediaserver-volume
              mountPath: "/downloads"
              subPath: "{{ .Values.general.storage.subPaths.downloads }}"
            - name: mediaserver-volume
              mountPath: "/movies"
              subPath: "{{ .Values.general.storage.subPaths.movies }}"
          {{- with .Values.overserr.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        {{- if not .Values.general.storage.customVolume }}
        - name: mediaserver-volume
          persistentVolumeClaim:
            claimName: {{ .Values.general.storage.pvcName }}
        {{- else }}
        - name: mediaserver-volume
          {{- toYaml .Values.general.storage.volumes | nindent 10 }}
        {{- end }}
        {{- if .Values.overserr.volume }}
        - name: {{ .Values.overserr.volume.name }}
          persistentVolumeClaim:
            claimName: {{ .Values.overserr.volume.name }}
        {{- end }}
        - name: init-files-overserr
          configMap:
            defaultMode: 493
            name: init-overserr-cm
      {{- with .Values.general.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
### SERVICES
apiVersion: v1
kind: Service
metadata:
  name: overserr
  labels:
    {{- include "k8s-mediaserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.overserr.service.type }}
  ports:
    - port: {{ .Values.overserr.service.port }}
      targetPort: {{ .Values.overserr.container.port }}
      protocol: TCP
{{ if eq .Values.overserr.service.type "NodePort" }}
      nodePort: {{ .Values.overserr.service.nodePort }}
{{ end }}
      name: overserr-port
  selector:
    app: overserr

---
{{ if .Values.overserr.service.extraLBService }}
apiVersion: v1
kind: Service
metadata:
  name: overserr-lb
  annotations:
    {{- include .Values.overserr.service.extraLBService.annotations . | nindent 4 }}
  labels:
    {{- include "k8s-mediaserver.labels" . | nindent 4 }}
spec:
  type: LoadBalancer
  ports:
    - port: {{ .Values.overserr.service.port }}
      targetPort: {{ .Values.overserr.container.port }}
      protocol: TCP
      name: overserr-port
  selector:
    app: overserr
{{ end }}
---
### INGRESS ###
{{ if .Values.overserr.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: overserr
  labels:
    {{- include "k8s-mediaserver.labels" . | nindent 4 }}
  {{- with .Values.overserr.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if .Values.overserr.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.general.ingress_host | quote }}
      secretName: {{ .Values.overserr.ingress.tls.secretName }}
{{ end }}
  ingressClassName: {{ .Values.general.ingress.ingressClassName }}
  rules:
    - host: {{ .Values.general.ingress_host | quote }}
      http:
        paths:
          - path: {{ .Values.overserr.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: overserr
                port:
                  number: {{ .Values.overserr.service.port }}
{{ end }}
{{ end }}
